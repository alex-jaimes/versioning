name: build-application

on: [push]

jobs:
  build-app:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: |
          source version
          echo "Version: $major.$minor.$patch.${{ github.run_number }}"
